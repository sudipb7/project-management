{
  "name": "columnz",
  "private": true,
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "lint": "turbo lint",
    "prettier": "prettier --write \"**/*.{ts,tsx,js,mjs,json,yaml,css,md}\"",
    "db:generate": "pnpm -F server db:generate",
    "db:migrate": "pnpm -F server db:migrate",
    "db:studio": "pnpm -F server db:studio"
  },
  "devDependencies": {
    "prettier": "^3.2.5",
    "turbo": "^2.0.7",
    "typescript": "^5.4.5"
  },
  "packageManager": "pnpm@8.15.6",
  "engines": {
    "node": ">=18"
  }
}
